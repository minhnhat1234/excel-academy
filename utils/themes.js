import { createMuiTheme } from '@material-ui/core'

const brightTheme = createMuiTheme({
    palette: {
      primary: {
        main: '#ffcc5c'
      },
  
      secondary: {
        main: '#ff914d'
      }
    },
  })

export { brightTheme }